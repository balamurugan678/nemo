(function (o) {
    "use strict";

    //function setUpTempLocalStorage(toOrFrom, attrName) {
    //    var tempJpLocalStorage = [[]];
    //    var modes = $(".fast-plan").attr("data-modes");
    //    if (modes) {
    //        tempJpLocalStorage.push([toOrFrom, attrName, modes]);
    //    }
    //    tfl.storage.set("tempJPLocalStorage", tempJpLocalStorage);
    //};

    function init() {
        // click events
        //$(".to-here").click(function () {
        //    setUpTempLocalStorage("#To", "data-to-modes");
        //});
        //$(".from-here").click(function () {
        //    setUpTempLocalStorage("#From", "data-from-modes");
        //});
    };

    init();

}(window.tfl.fastPlan = window.tfl.fastPlan || {}));